# auto-generated by grc.converter

id: ettus_rfnoc_register_probe
label: 'RFNoC: Register Probe'

parameters:
-   id: logger_id
    label: RFNoC Logger ID
    dtype: string
    default: rfnoc_register_logger_0
-   id: block_id
    label: RFNoC Block ID
    dtype: string
    default: rfnoc_block_0
-   id: registers
    label: Register List
    dtype: ''
    default: '["reg0","reg1"]'

templates:
    imports: from ettus import rfnoc_register_probe
    make: |-
        <% logger = 'self' + ('.' + logger_id if logger_id else '') %>
        <% block = 'self' + ('.' + block_id if block_id else '') %>
        ${logger}.add_query(${block}, ${registers})

file_format: 1
