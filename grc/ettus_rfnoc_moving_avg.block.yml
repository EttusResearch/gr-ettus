# auto-generated by grc.converter

id: ettus_rfnoc_streamer_moving_avg
label: 'RFNoC: Moving Average'

parameters:
-   id: length
    label: Average length
    dtype: int
    default: '10'
-   id: block_index
    label: Block Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(block_index) < 0 else 'none') }
-   id: device_index
    label: Device Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(device_index) < 0 else 'none') }
-   id: fpga_module_name
    label: FPGA Module Name
    category: RFNoC Config
    dtype: string
    default: noc_block_moving_avg
    hide: all

inputs:
-   domain: rfnoc
    dtype: complex
-   domain: message
    id: rfnoc
    optional: true

outputs:
-   domain: rfnoc
    dtype: complex
asserts:
- ${ length in range(1, 256) }

templates:
    imports: import ettus
    make: |-
        ettus.rfnoc_generic(
            self.device3,
            uhd.stream_args( # TX Stream Args
                cpu_format="fc32", # TODO: This must be made an option
                otw_format="sc16",
                args="",
            ),
            uhd.stream_args( # RX Stream Args
                cpu_format="fc32", # TODO: This must be made an option
                otw_format="sc16",
                args="",
            ),
            "MovingAverage", ${block_index}, ${device_index},
        )
        self.${id}.set_arg_int("length", ${length})
    callbacks:
    - set_arg_int("length", ${length})

file_format: 1
