# auto-generated by grc.converter

id: ettus_rfnoc_streamer_packet_resizer
label: 'RFNoC: Packet Resizer'

parameters:
-   id: pkt_size
    label: Packet Size
    dtype: int
    default: '256'
-   id: block_index
    label: Packet Resizer Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(block_index) < 0 else 'none') }
-   id: device_index
    label: Device Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(device_index) < 0 else 'none') }
-   id: fpga_module_name
    label: FPGA Module Name
    category: RFNoC Config
    dtype: string
    default: noc_block_packet_resizer
    hide: all

inputs:
-   domain: rfnoc
    dtype: complex

outputs:
-   domain: rfnoc
    dtype: complex

templates:
    imports: import ettus
    make: |-
        ettus.rfnoc_generic(
            self.device3,
            uhd.stream_args( # TX Stream Args
                cpu_format="fc32", # TODO: This must be made an option
                otw_format="sc16",
                args="pkt_size=$pkt_size",
            ),
            uhd.stream_args( # RX Stream Args
                cpu_format="fc32", # TODO: This must be made an option
                otw_format="sc16",
                args="",
            ),
            "PacketResizer", ${block_index}, ${device_index},
        )

file_format: 1
