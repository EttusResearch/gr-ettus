# auto-generated by grc.converter

id: variable_ettus_device3
label: Device3
flags:
- show_id

parameters:
-   id: dev_addr
    label: Device Address
    dtype: string
    default: type=x300
    hide: ${ ('none' if dev_addr else 'part') }
-   id: dev_args
    label: Device Arguments
    dtype: string
    default: ''
    hide: ${ ('none' if dev_args else 'part') }
value: ${ dev_addr }

templates:
    imports: |-
        from gnuradio import uhd
        import ettus
    var_make: self.device3 = ${id} = ettus.device3(uhd.device_addr_t( ",".join((${dev_addr}, ${dev_args})) ))

file_format: 1
