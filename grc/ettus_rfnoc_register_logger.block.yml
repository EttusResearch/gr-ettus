# auto-generated by grc.converter

id: ettus_rfnoc_register_logger
label: 'RFNoC: Register Logger'

parameters:
-   id: poll_rate
    label: Poll Rate (Hz)
    dtype: real
    default: '1'
-   id: csvfile
    label: Filename
    dtype: ''
    default: '"probe.csv"'
-   id: enable
    label: Enable Logger
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Enabled, Disabled]
-   id: debug
    label: Console Output
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Enabled, Disabled]

templates:
    imports: from ettus import rfnoc_register_probe
    make: |-
        rfnoc_register_probe.rfnoc_register_probe(1.0 / (${poll_rate}), ${enable}, ${csvfile}, ${debug})
        self.temp_${id}_stop = self.stop
        def stop_${id}_fcn():
            self.${id}.shutdown()
            self.temp_${id}_stop()
        self.stop = stop_${id}_fcn

file_format: 1
